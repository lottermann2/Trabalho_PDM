<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <EditText android:id="@+id/activity_cadastro_nome"
        android:layout_width="match_parent"
        android:layout_margin="8dp"
        android:hint="Nome"
        android:inputType="textCapWords"
        android:layout_height="wrap_content" />

    <EditText android:id="@+id/activity_cadastro_email"
        android:layout_width="match_parent"
        android:layout_margin="8dp"
        android:hint="E-mail"
        android:inputType="textEmailAddress"
        android:layout_height="wrap_content" />

    <EditText android:id="@+id/activity_cadastro_senha"
        android:layout_width="match_parent"
        android:layout_margin="8dp"
        android:hint="Senha"
        android:inputType="textPassword"
        android:layout_height="wrap_content" />

    <Button android:id="@+id/activity_cadastro_botao_cadastrar"
        android:layout_width="match_parent"
        android:layout_margin="8dp"
        android:onClick="cadastrarClick"
        android:text="Cadastrar"
        android:layout_height="wrap_content" />

</LinearLayout>